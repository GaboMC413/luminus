# Git Workflow Rule

- DO NOT automatically make git commits after editing files.
- ONLY edit code files in the workspace as requested.
- NEVER execute `git commit` or `git add` + `git commit` unless the user explicitly asks for a commit (e.g. "haz un commit", "hagamos el commit").
