# Reglas del Proyecto LUMINUS (AGENTS.md)

## Reglas de Protección de Archivos Estrictas

- **PROHIBIDO MODIFICAR `amplify.yml`**:
  - El archivo `amplify.yml` ubicado en la raíz del repositorio **NUNCA** debe ser editado o modificado por el agente bajo ninguna circunstancia.
  - El contenido de `amplify.yml` debe mantenerse exactamente igual al que está en la rama `main`.
