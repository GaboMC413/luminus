version: 1
applications:
  - appRoot: apps/platform
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm ci --cache .npm --prefer-offline
        build:
          commands:
            - printf "DATABASE_URL=%s\nAUTH_SESSION_SECRET=%s\nNEXT_PUBLIC_GOOGLE_MAPS_API_KEY=%s\nS3_AVATAR_BUCKET=%s\nS3_AVATAR_REGION=%s\nS3_AVATAR_PUBLIC_BASE_URL=%s\nS3_AVATAR_ACCESS_KEY_ID=%s\nS3_AVATAR_SECRET_ACCESS_KEY=%s\nSES_REGION=%s\nSES_FROM_EMAIL=%s\nSES_ACCESS_KEY_ID=%s\nSES_SECRET_ACCESS_KEY=%s\nENABLE_RUNTIME_DEBUG=%s\n" "$DATABASE_URL" "$AUTH_SESSION_SECRET" "$NEXT_PUBLIC_GOOGLE_MAPS_API_KEY" "$S3_AVATAR_BUCKET" "$S3_AVATAR_REGION" "$S3_AVATAR_PUBLIC_BASE_URL" "$S3_AVATAR_ACCESS_KEY_ID" "$S3_AVATAR_SECRET_ACCESS_KEY" "$SES_REGION" "$SES_FROM_EMAIL" "$SES_ACCESS_KEY_ID" "$SES_SECRET_ACCESS_KEY" "$ENABLE_RUNTIME_DEBUG" > apps/platform/.env.production
            - npm run build:platform
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - ../../.npm/**/*
          - ../../node_modules/**/*

  - appRoot: apps/marketing
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../..
            - npm ci --cache .npm --prefer-offline
        build:
          commands:
            - npm run build:marketing
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - ../../.npm/**/*
          - ../../node_modules/**/*
